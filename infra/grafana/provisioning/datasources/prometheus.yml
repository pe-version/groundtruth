apiVersion: 1

# Auto-provisions the Prometheus datasource so an out-of-the-box Grafana
# is wired up the moment the observability profile starts.
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
